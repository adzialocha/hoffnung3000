import React, { Component } from 'react'

import { TicketWizard } from '../components'

class Tickets extends Component {
  render() {
    return (
      <section>
        <TicketWizard />
      </section>
    )
  }
}

export default Tickets
